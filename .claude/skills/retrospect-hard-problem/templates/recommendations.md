---
title: "Tenet / skill recommendations — <PROBLEM THEME>"
date: <YYYY-MM-DD>
derived_from: <path to the companion retrospective.md>
status: PROPOSED — owner review required before any edit is applied
generated_by: retrospect-hard-problem skill
---

<!--
These are PROPOSALS expressed as diffs-in-prose. This skill does NOT apply them.
Each carries an evidence tier (A/B/C); only A/B may be proposed. Cross-reference
the exact tenet number / skill / memory file each touches so the owner can act
in one pass.
-->

# Recommendations

## 1. New / amended tenets

For each: which repo's `CLAUDE.md`, the exact tenet to add or amend, the
recurring mistake it prevents (cite the retrospective §4 row), and the tier.

| # | Repo(s) | Tenet (new or amend #N) | Prevents (retro §4 row) | Tier |
|---|---------|-------------------------|-------------------------|------|
| | | | | |

> Proposed wording (per row, diffs-in-prose):
> - **R1 (qiyas, amend Tenet N):** "<the added/changed sentence>" — because <§4 row>.

## 2. New memory files (un-codified recurring mistakes)

Every `✗ NEVER codified` row from retrospective §4 that recurred ≥2× across a
date-spread becomes a proposed `memory/feedback_*.md`. Give the slug, the
one-line fact, the **Why**, and the **How to apply**.

| Proposed file | Fact | Why it recurred | Tier |
|---------------|------|-----------------|------|
| `memory/feedback_<slug>.md` | | | |

## 3. New / edited skills

Where a recurring mistake happened *inside* a loop a skill already governs
(`iterate-construction-hypothesis`, `iterate-detector-calibration`,
`handle-falsification`, `present-options`, `escalate-qiyas-divergence`), propose
the guard to add to that skill rather than a new tenet.

| Skill | Guard to add | Prevents | Tier |
|-------|--------------|----------|------|
| | | | |

## 4. Approach-level recommendations (beyond tenets/skills)

Structural moves the retrospective surfaced (e.g. a producer-side primitive that
dissolves a class of detector mistakes; an alternative the team never tried).
Each: action, expected payoff, the §3/§5 finding it answers, tier.

1. ...

---

## Application checklist (for the owner, after approval)

- [ ] Tenet edits applied to the named `CLAUDE.md` files (mirror cross-repo where the tenet is shared).
- [ ] New `memory/feedback_*.md` files written + linked from each repo's `MEMORY.md`.
- [ ] Skill guards added; skill `description` updated if the trigger surface changed.
- [ ] This recommendations doc marked `status: APPLIED <date>` with the commit sha.
